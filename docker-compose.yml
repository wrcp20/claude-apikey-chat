services:
  backend:
    build: ./backend
    container_name: claude-apikey-backend
    ports:
      - "${HOST_PORT:-3200}:3200"
    env_file:
      - .env
    environment:
      - PORT=3200
      - NODE_ENV=production
      - ALLOWED_ORIGINS=*
    restart: unless-stopped
